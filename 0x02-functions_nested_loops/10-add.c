#include "main.h"
#include <stdio.h>

/**
 * add - adds two integers and returns the result
 * @a: first operand
 * @b: second operand
 *
 * Return: Sum
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
